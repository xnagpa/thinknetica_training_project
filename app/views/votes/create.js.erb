
<% if @vote.errors.present?%>
			<% @vote.errors.full_messages.each do |message|%>
			$('.vote_errors').html('<%= j message %>').css('color','red');
		<%end%>
	<%else%>		
	var s = "#<%=@votable.class.name.downcase%>-<%=@votable.id%> .rating p";
	var links = "#vote-<%=@votable.class.name.downcase%>-<%=@votable.id%>";
   alert("Fuck!!");
   $(s).html("Rating: <%=@votable.rating%>");
   $(s).css("color","red");
   $(links).css("color","red");   
   l= "<%= j link_to 'Revote', "#{vote_path(@vote.id)}", method: :delete, remote: :true, class: 'destroy_vote btn btn-default',role:'button'%>";
   $(links).html(l);
   

	<%end%>	


	 // =link_to "Revote", vote_path(previous_vote), method: :delete, remote: true, class: 'destroy_vote'
  //  =link_to "Thumb up", polymorphic_path([votable, Vote.new], vote:{score:1}), method: :post, remote: true, class: 'score'
  //  =link_to "Thumb down", polymorphic_path([votable, Vote.new], vote:{score:-1} ), method: :post, remote: true,class: 'unscore' 