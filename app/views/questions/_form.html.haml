= semantic_form_for question, remote: true, html: { id: "edit-question-#{question.id}"} do |f|
 = f.inputs do
  = f.input :title         
  = f.input  :content
  = f.semantic_fields_for :attachments do |a| 
   = render 'attachment_fields', f: a      
  .links
   = link_to_add_association 'add attachment', f, :attachments

  = f.submit "Save"
    
    


